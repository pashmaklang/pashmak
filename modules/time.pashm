
namespace time;

    func time;
        py 'self.mem = time.time()';
    endfunc;

    func sleep;
        $time_to_sleep = ^;
        py 'self.mem = time.sleep(' + str($time_to_sleep) + ')';
    endfunc;

endnamespace;
