
namespace random;

    func randint;
        set $args; copy $args;
        py 'self.mem = random.randint(' + str($args[0]) + ',' + str($args[1]) + ')';
    endfunc;

    func random;
        py 'self.mem = random.random()';
    endfunc;

endnamespace;
