
namespace random;

    func randint ($args);
        py 'self.mem = random.randint(' + str($args[0]) + ',' + str($args[1]) + ')';
    endfunc;

    func random;
        py 'self.mem = random.random()';
    endfunc;

endnamespace;
